# Admin app uses this to call the API
NEXT_PUBLIC_API_BASE=http://localhost:4000

# Gate the whole admin with a secret link:
# Open https://<admin-domain>/?k=<ADMIN_ACCESS_KEY> once, then cookie is set.
ADMIN_ACCESS_KEY=change-me

# Optional: enable real credential check on /login
ADMIN_LOGIN_EMAIL=team@virgoschool.com
ADMIN_LOGIN_PASSWORD=change-me-too
